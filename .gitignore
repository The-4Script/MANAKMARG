# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Secrets / local configuration ---
.env
.env.*
!.env.example
*.pem
*.key
.claude/

# --- Generated, cached or private data ---
# The supplied BIS Excel exports in data/*.xlsx and data/manifests/ are raw inputs and stay tracked.
# The runtime data bundle (deploy/data/manakmarg-data.tar.gz) is tracked separately; see docs/DEPLOYMENT.md.
data/raw/
data/staging/
data/processed/
data/indexes/
data/uploads/
data/documents/
data/bundles/
# Weekly BIS refresh: downloaded exports, staged datasets, snapshots and the refresh log (docs/DATA_REFRESH.md).
data/refresh/
# Supplied HSN master: its publisher/terms are not stated, so it is not redistributed through git. Its rows ship in the
# data bundle; place the workbook in data/ to re-run `manakmarg ingest --steps hsn`.
data/HSN.xlsx
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
*.joblib

# --- Frontend ---
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo

# --- OS / editors ---
.DS_Store
Thumbs.db
.vscode/
.idea/
